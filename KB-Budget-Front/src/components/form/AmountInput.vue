<script setup>
/**
 * AmountInput.vue — 금액 입력 (핵심)
 *
 * 역할
 * - 숫자만 허용, 천 단위 표시, 최소값 검증 등.
 *
 * 구현 요구사항
 * - v-model로 number 또는 문자열 중 하나로 통일(저장 시 number로 변환).
 * - 입력 중 콤마 표시 등 UX는 선택; blur 시 포맷 정리.
 * - 0 이하 방지, 최대값 제한(선택).
 * - 모바일에서 inputmode="numeric" 또는 "decimal" 고려.
 */
</script>

<template>
  <!--
    label + input, 에러 메시지 영역
  -->
  <div class="amount-input">
    <!-- 구현 -->
  </div>
</template>

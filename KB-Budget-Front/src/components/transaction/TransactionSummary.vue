<script setup>
/**
 * TransactionSummary.vue — 필터 기준 요약 (선택)
 *
 * 역할
 * - 현재 필터/선택 월에 대한 합계·건수 등을 HomeView 상단 또는 하단에 요약 카드로 표시.
 *
 * 구현 요구사항
 * - store의 getter(예: summaryForCurrentFilter)를 사용하거나, props로 숫자만 받기.
 * - StatsView의 월별 요약과 수치 정의가 겹치지 않게 역할 분리(여기는 "조회 화면용 스냅샷").
 * - 생략 가능: 과제 범위에 없으면 컴포넌트 파일만 두고 미사용해도 됨.
 */
</script>

<template>
  <div class="transaction-summary">
    <!-- 구현 (선택) -->
  </div>
</template>
